[project]
name = "livescreen-translator"
version = "1.0.0"
description = "Real-time Chinese -> English screen overlay translator using PaddleOCR + local Ollama"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
